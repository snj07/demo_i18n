class GlobalKeys {
  ///  To avoid accidentally instantiation using the implicit default constructor.
  GlobalKeys._();

  static const String currentLocaleKey = 'currentLocale';
  static const String currentThemeKey = 'currentTheme';
}
